from setuptools import setup, find_packages

setup(
    name = 'pymir',
    version = '0.1',
    author = 'Tom Crowther',
    description = 'Simple python wrapper for MIRIAD',
    packages = find_packages(),
    package_data = {},

)
